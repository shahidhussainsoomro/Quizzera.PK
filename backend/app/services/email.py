from typing import Iterable


def send_email(subject: str, recipients: Iterable[str], body: str) -> None:
    """Placeholder email sender.

    Replace with real email service integration (SMTP/SendGrid/etc.).
    """
    del subject, recipients, body
